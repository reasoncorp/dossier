%table
  %thead
    %tr
      - @report.results.first.keys.each do |header|
        %th= header
  %tbody
    - @report.results.each do |row|
      %tr
        - row.values.each do |value|
          %td= value
