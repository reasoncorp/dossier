defaults: &defaults
  adapter: mysql2
  database: dossier_test
  host: localhost
  username: root
  password: rubyr3d!
  
development:
  <<: *defaults
test:
  <<: *defaults
production:
  <<: *defaults
